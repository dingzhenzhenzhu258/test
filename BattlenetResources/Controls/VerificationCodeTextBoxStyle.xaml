<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:Controls="clr-namespace:BattlenetResources.Controls">

    <!--
        如果你自定义了一个类 VerificationTextBox : TextBox，系统会认为它是一个全新的类型。
        
        如果不写 BasedOn：你的自定义控件会“丢掉”普通 TextBox 所有的默认属性（比如边框、背景颜色、右键菜单等）。它会变成一个“裸”的控件，直到你在样式里重新定义它们。
        
        如果写了 BasedOn="{StaticResource {x:Type TextBox}}"：你的控件会先继承系统（或你项目里定义的）TextBox 的所有 Setter，然后再应用你自定义的 Template。这能保证你的控件在基础行为上和普通 TextBox 保持一致。
    -->
    <Style BasedOn="{StaticResource {x:Type TextBox}}" TargetType="{x:Type Controls:VerificationTextBox}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="White" />
        <Setter Property="CaretBrush" Value="White" />
        <Setter Property="MinHeight" Value="42" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TextBoxBase}">
                    <Border
                        Name="Border"
                        Padding="8"
                        Background="Transparent"
                        BorderBrush="#6B6C70"
                        BorderThickness="1"
                        CornerRadius="3">
                        <!--
                            在 TextBox 的模板中，必须有一个名为 PART_ContentHost 的元素（通常是 ScrollViewer 或 AdornerDecorator）。
                            WPF 内部逻辑会自动找到这个名字，并将你输入的文字、光标渲染在这里
                        -->
                        <DockPanel>
                            <Button
                                BorderBrush="#3D91F7"
                                Content="获取验证码"
                                DockPanel.Dock="Right">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="Transparent">
                                            <ContentPresenter
                                                x:Name="TextHolder"
                                                HorizontalAlignment="Center"
                                                VerticalAlignment="Center"
                                                TextElement.Foreground="{TemplateBinding BorderBrush}" />
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <EventTrigger RoutedEvent="Button.Click">
                                                <EventTrigger.Actions>
                                                    <BeginStoryboard>
                                                        <Storyboard>
                                                            <ColorAnimation
                                                                Storyboard.TargetName="TextHolder"
                                                                Storyboard.TargetProperty="(TextElement.Foreground).(SolidColorBrush.Color)"
                                                                To="#888888"
                                                                Duration="0:0:0" />

                                                            <ColorAnimation
                                                                BeginTime="0:0:2"
                                                                Storyboard.TargetName="TextHolder"
                                                                Storyboard.TargetProperty="(TextElement.Foreground).(SolidColorBrush.Color)"
                                                                To="#3D91F7"
                                                                Duration="0:0:0" />
                                                        </Storyboard>
                                                    </BeginStoryboard>
                                                </EventTrigger.Actions>
                                            </EventTrigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                            <Grid>
                                <ScrollViewer
                                    x:Name="PART_ContentHost"
                                    Margin="5,0"
                                    VerticalAlignment="Center"
                                    VerticalContentAlignment="Center"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    FontWeight="Bold"
                                    Foreground="White" />

                                <TextBlock
                                    x:Name="PlaceholderText"
                                    Margin="12,0,0,0"
                                    VerticalAlignment="Center"
                                    FontWeight="Normal"
                                    Foreground="#666666"
                                    IsHitTestVisible="False"
                                    Text="请输入验证码"
                                    TextElement.FontWeight="Normal">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Text, RelativeSource={RelativeSource TemplatedParent}}" Value="">
                                                    <Setter Property="Visibility" Value="Visible" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Text, RelativeSource={RelativeSource TemplatedParent}}" Value="{x:Null}">
                                                    <Setter Property="Visibility" Value="Visible" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Grid>
                        </DockPanel>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="BorderBrush" Value="White" />
                        </Trigger>
                        <Trigger SourceName="PART_ContentHost" Property="IsKeyboardFocusWithin" Value="True">
                            <Setter TargetName="Border" Property="BorderBrush" Value="#63AEE4" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>